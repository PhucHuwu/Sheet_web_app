# Patch files for Univer Server configuration

# These files will be copied over the downloaded Univer package

This folder contains configuration files that override the default Univer Server settings.

Files:

-   `configs/config.yaml` - Database and USIP configuration
-   `.env.custom` - Custom environment variables

These will be applied automatically by the install script.
